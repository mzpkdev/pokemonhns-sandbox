{
  "name": "@pokemonhns/devtooling-map-render",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "hns-map-render": "./dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "check": "npm run format && npm run lint && npm run typecheck",
    "clean": "wa clean",
    "dev": "tsc --project tsconfig.build.json --watch",
    "format": "wa format --check",
    "lint": "wa lint",
    "render": "npm run build && node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pokemonhns/devtooling-core": "file:../../packages/core",
    "cmdore": "0.4.1"
  }
}
